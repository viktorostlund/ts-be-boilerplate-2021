export interface Person {
    first_name: string,
    last_name: string,
    gender: string,
    date_of_birth: string,
    country_of_birth: string
}

export interface Car {
    make: string,
    model: string,
    price: number,
}
